import 'package:flutter/material.dart';

class AppColors {
  // A dark background to provide a sleek, modern look
  static const Color primary = Color(0xFF121212);
  // A cool blue accent that stands out without being too harsh
  static const Color accent = Color(0xFF1E88E5);
  // A slightly brighter blue for interactive elements like buttons
  static const Color button = Color(0xFF1976D2);
  // Google icons often use a recognizable blue, which you can keep for consistency
  static const Color googleIcon = Color(0xFF4285F4);
}
